# PyTorch documentation moved

The PyTorch **install** steps, **conda** environment (`nanobragg_torch`), and **pytest** environment variables now live in:

- **[INSTALL.md](INSTALL.md)** — PyTorch § (conda, `pip install -e .`, CUDA wheels, verification)
- **[README.md](README.md)** — **PyTorch implementation** (CLI quick start, tests, troubleshooting)

Longer background (C vs PyTorch design, autograd, extended CLI examples, benchmarks) is in **[docs/pytorch_guide.md](docs/pytorch_guide.md)**.

This file is kept so older links to `README_PYTORCH.md` still resolve.
